<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <Button
        android:id="@+id/btnBlog"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/pading150"
        android:text="@string/button_post"
        app:layout_constraintEnd_toEndOf="@+id/btnSerie"
        app:layout_constraintStart_toStartOf="@+id/btnSerie"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/btnSerie"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="@string/button_serie"
        app:layout_constraintBottom_toTopOf="@+id/btnConcurso"
        app:layout_constraintEnd_toEndOf="@+id/btnConcurso"
        app:layout_constraintStart_toStartOf="@+id/btnConcurso"
        app:layout_constraintTop_toBottomOf="@+id/btnBlog" />

    <Button
        android:id="@+id/btnConcurso"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/pading150"
        android:text="@string/button_concurso"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>